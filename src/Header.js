import React from 'react';
import './App.css';

const Header = ({}) => {
  return (
    <div className='app-header'>
      <h1>EduChat</h1>
    </div>
  );
};

Header.propTypes = {
};

export default Header;
